from setuptools import setup, find_packages

setup(
    name="flight_fare_prediction",
    version="1.0.0",
    description="Flight Fare Prediction Using Machine Learning",
    packages=find_packages(where="."),
    python_requires=">=3.9",
)
